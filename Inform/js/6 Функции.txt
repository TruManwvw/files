Функции 

1) ИМЯ
  show... - показать
  get... - возвражает значение
  calc... - вычисляет
  create... - создаёт
  check... - проверяет

2) ОБЪЯВЛЕНИЕ
  function имя(параметры) {
      //тело функции
  }

3) return

4) рекурсия - функция вызывает саму себя

5) функциональное выражение
  let showMessage = function() {
      console.log('Привет')
  };

6) Стрелочная фунция
  let имя_переменной = (параметр,...параметр) => выражение
  
7) setTimeout - вызов один раз через определенный промежуток времени
   setInterval - вызов несколько раз --|--
   setInterval(функция, задержка, параметр)
   clearInterval(timeId)